class Food {
  final String id, name, imagepath, category;
  final double price, discount, rating;
  Food(
      {this.id,
      this.name,
      this.imagepath,
      this.category,
      this.price,
      this.discount,
      this.rating});
}
